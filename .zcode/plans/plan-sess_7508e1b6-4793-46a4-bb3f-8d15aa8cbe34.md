# 选股引导设计方案 → 先落文档（本期不改代码）

把已评审定案的选股引导设计写入工程文档，作为后续编码的 SSOT。

## 落点（按 stock-calculator-docs 规范）

1. **新增 `docs/guide/design.md`**（核心交付）：按 docs skill 规范写 frontmatter（status/updated），内容：
   - 背景与定位（无流程痛点；方案落 main；已确认取向：引擎 REST + 聊天引导一次到位、终点=个股档案）；
   - 引导流程（两步向导 mermaid + 文字）：Step1 `POST /api/guide/analyze-message`（快/慢路径、锚定丢弃不编造、题材两跳、nextStep 首位）；Step2 `GET /api/guide/stock-brief`（提及/题材归属/公告摘要聚合、nextSteps 首位）；聊天入口（dispatch REST_SEEDS + 零命中自纠闭环）；
   - API 契约表（两工具：入参/返回/ ToolInvoker 形态——POST body 与 GET query 的原因）；
   - 改动清单（8 项，标注「待实施」）：crawler 基包 3 方法+native 查询、search 基包 StockProfileApi 门面、guide 新域结构、orchestration REST_SEEDS+2、data.sql 播种、测试与验证口径；
   - **实施红线两条**（评审定案）：①两跳 native SQL 索引结论（已核验 idx_cas_stock_id/idx_csub_subject_id/主键 join 覆盖，落地后 EXPLAIN 验证）；②keep_head 截断安全（nextStep/nextSteps 置 JSON 顶层首位、候选 top8、样例仅 title+ctime）；
   - 决策记录表（D1-Dn：为何落 main、为何无状态无表、为何 GET query 非 path variable、为何 v1 不动 copilot 系统提示词、未锚定丢弃原则等）；
   - P2 展望（技术面快照/提醒联动/KG 时间线/聊天系统提示词模板/前端向导 UI）。
   - 文末标注：设计已评审（2026-09-25），代码未落地；按本文档实施即可。
2. **docs/README.md**：guide 域条目加入域头索引。
3. **stock-calculator-service-index skill**（项目内 .agents/skills/）：归属表加 guide 行（子包结构规划 + 文档落点 docs/guide/）。
4. **stock-calculator-docs skill**（~/.agents/skills/）：§〇 域表同步 guide 域。
5. **自检**：跑 `sh scripts/agent-tools/docs-index-lint.sh` 与 `sh scripts/agent-tools/index-lint.sh`，全绿收尾。

## 不做

不改任何 Java 代码 / SQL / 配置；数据表、控制器等实现留待按文档施工。